### Instalando os pacotes necessários para trabalhar no software durante o curso

install.packages(c("ggplot2","swirl","lattice","car","fImport","tseries"))

# Carregando os pacotes para utilização nesses script

library(ggplot2)
library(swirl)
library(lattice)
library(car)
library(fImport)
library(tseries)

# Uma maneira alternativa de realizar esse conjunto de instalações é:

#if(!require(pacman)){install.packages("pacman")}#
#pacman::p_load(ggplot2, swirl, lattice, car, fImport, tseries)#

